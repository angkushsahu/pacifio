import { ToastOptions } from "react-toastify";

const toastOptions: ToastOptions = {
	position: "top-center",
	style: {
		background: "#191919",
		color: "#FFFFFF",
	},
};

export default toastOptions;
